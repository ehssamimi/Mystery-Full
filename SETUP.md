# راهنمای راه‌اندازی سیستم احراز هویت

## مراحل راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
npm install
```

### 2. تنظیم دیتابیس

پروژه از **PostgreSQL** استفاده می‌کند (برای Development و Production).

#### گزینه ۱: استفاده از PostgreSQL محلی

اگر PostgreSQL روی سیستم شما نصب است:

```bash
# ایجاد دیتابیس
createdb mystery_full

# تنظیم DATABASE_URL در .env.local
DATABASE_URL="postgresql://username:password@localhost:5432/mystery_full?schema=public"
```

#### گزینه ۲: استفاده از Vercel Postgres (پیشنهادی برای ایران 🇮🇷)

**برای Development:**
می‌توانید از همان Vercel Postgres که برای Production استفاده می‌کنید، برای Development هم استفاده کنید:

1. در [Vercel Dashboard](https://vercel.com/dashboard) یک پروژه موقت بسازید (یا از همان پروژه اصلی استفاده کنید)
2. به بخش **Storage** بروید
3. روی **Create Database** کلیک کنید
4. **Postgres** را انتخاب کنید
5. یک نام برای دیتابیس انتخاب کنید
6. بعد از ایجاد، Connection String را کپی کنید
7. در `.env.local` قرار دهید:

```bash
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&sslmode=require"
```

**مزایا:**
- ✅ بدون نیاز به فیلترشکن (همان Vercel است)
- ✅ رایگان برای شروع
- ✅ یکسان با Production (بدون تفاوت در تنظیمات)
- ✅ Backup خودکار
- ✅ مناسب برای کاربران ایرانی

**گزینه‌های جایگزین (اگر Vercel Postgres در دسترس نبود):**
- **[Railway](https://railway.app)** - PostgreSQL رایگان، معمولاً فیلتر نیست
- **[Render](https://render.com)** - PostgreSQL رایگان، معمولاً فیلتر نیست

**نکته:** فایل `.env.local` را به Git Commit نکنید (در `.gitignore` است).

### 3. ایجاد Migration و Seed

```bash
# Generate Prisma Client
npm run prisma:generate

# Create database and run migrations
npm run prisma:migrate

# Seed database with games
npm run prisma:seed
```

### 4. ایجاد اولین کاربر ادمین

برای ایجاد اولین کاربر ادمین، می‌توانید از Prisma Studio استفاده کنید:

```bash
npm run prisma:studio
```

یا می‌توانید یک script ساده بنویسید که یک کاربر با نقش admin ایجاد کند.

### 5. اجرای پروژه

```bash
# Development
npm run dev

# Production
npm run build
npm start
```

## نحوه استفاده

### لاگین

1. به آدرس `/login` بروید
2. شماره تلفن خود را وارد کنید (فرمت: 09123456789)
3. کد تأیید را وارد کنید (دیفالت: `111111`)
4. بعد از لاگین:
   - کاربر عادی → به صفحه اصلی (`/`) هدایت می‌شود
   - ادمین → به داشبورد (`/admin/dashboard`) هدایت می‌شود

### پنل ادمین

بعد از لاگین به عنوان ادمین، می‌توانید:

1. **مدیریت کاربران** (`/admin/dashboard/users`):
   - مشاهده لیست تمام کاربران
   - تغییر نقش کاربران (user/admin)

2. **مدیریت بازی‌ها** (`/admin/dashboard/games`):
   - مشاهده لیست تمام بازی‌ها
   - ویرایش اطلاعات بازی‌ها

## نکات مهم

- کد تأیید دیفالت: `111111`
- Session tokens به مدت 30 روز معتبر هستند
- کاربران نمی‌توانند نقش خود را تغییر دهند
- صفحات ادمین فقط برای کاربران با نقش `admin` قابل دسترسی هستند

## Deploy به Vercel

برای راهنمای کامل استقرار، به فایل **[DEPLOY.md](./DEPLOY.md)** مراجعه کنید.

خلاصه مراحل:
1. پروژه را به GitHub push کنید
2. در Vercel، پروژه را import کنید
3. Vercel Postgres را اضافه کنید (DATABASE_URL به صورت خودکار تنظیم می‌شود)
4. Build به صورت خودکار انجام می‌شود (از `vercel.json` و `package.json`)
5. بعد از Deploy، Seed API را فراخوانی کنید: `/api/admin/seed`

## ساختار فایل‌ها

```
app/
├── (auth)/              # صفحات احراز هویت
│   └── login/
│       └── page.tsx
├── (admin)/             # صفحات ادمین
│   ├── dashboard/
│   │   ├── page.tsx
│   │   ├── users/
│   │   │   └── page.tsx
│   │   └── games/
│   │       └── page.tsx
│   └── layout.tsx
├── api/
│   ├── auth/            # API های احراز هویت
│   │   ├── login/
│   │   ├── verify/
│   │   ├── logout/
│   │   └── me/
│   └── admin/           # API های ادمین
│       ├── users/
│       └── games/
└── middleware.ts        # محافظت از صفحات

lib/
└── store/
    └── auth-store.ts    # Zustand store برای auth
```

